{
  "extends": ["react-app", "react-app/jest", "prettier"],
  "rules": {
    "semi": "warn" // if no semi colon then shows warning
  },
  "settings": {
    "react": {
      "version": "detect" // Tells eslint-plugin-react to automatically detect the version of React to use
    }
  }
}
